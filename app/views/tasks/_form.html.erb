<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "hata") %> var görev kaydedilemedi</h2>

      <ul>
        <% task.errors.full_messages.each do |message| %>
          <li class="list-group-item list-group-item-warning"><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name, placeholder: 'task name' %>
  </div>

  <div class="field">
    <%= form.text_field :description, placeholder: 'task description' %>
  </div>

  <div class="field">
    <%= form.date_select :deadline %>
  </div>

  <div class="field">
    <%= form.number_field :user_id, id: :friend_user_id, value: current_user.id, type: :hidden %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-info' %>
  </div>
<% end %>
